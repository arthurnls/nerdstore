<h1><%= @department.name %></h1>

<% if @products.empty? %>
    <p>There are no products found.</p>
<% else %>
    <p>Listing all <%= @products.count %> products</p>
    <div class="card-deck">
        <%= render @products %>
    </div>
<% end %>
